using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class CheckListUIMgr : MonoBehaviour {
    public Dictionary<string, GameObject> checkListDic = new Dictionary<string, GameObject>();
    public Person _cur;
    public string _curId;
    public Dictionary<string, Person> Info = new Dictionary<string, Person>();
    public Dictionary<string, Person> Infos = new Dictionary<string, Person>();
    public List<ITEM> s = new List<ITEM>();

    public Person Get(string key)
    {
        return Infos[key];
    }
   
}
